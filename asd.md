*Uoload*
git status
git add .
git commit -m "Update"
git push origin main

*Reset*
git fetch --all
git reset --hard origin/main

*Implement*
implement the next feautre (). than create and run tests for it and, update the docs/ to reflect the new feature and than update ther examples/
